# gRPC Client/Server in C++ 

Example of gRPC client and server in C++.

## Dependencies

- Docker

## How to Run Server

```bash
$ docker compose up -d
```

## How to Use Client

```bash
$ docker exec -it grpc bazel run client --help
```
